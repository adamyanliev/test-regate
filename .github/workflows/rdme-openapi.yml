# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-02-13T13:33:00.912Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi doc/public_api/reference/token.yaml --key=${{ secrets.README_API_KEY }} --id=63ea3becdec8ab00652b3e3b
      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi doc/public_api/reference/customer.yaml --key=${{ secrets.README_API_KEY }} --id=63ea3e7a17a9bd002110d7f4
      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi doc/public_api/reference/customer.yaml --key=${{ secrets.README_API_KEY }} --id=63ea3e9fb633000193654588


